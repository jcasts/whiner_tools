require "rubygems"
require "hoe"

Hoe.spec "whiner_tools" do
  developer "Jeremie Castagna", "yaksnrainbows@gmail.com"

  self.extra_rdoc_files = FileList["*.rdoc"]
  self.history_file     = "CHANGELOG.rdoc"
  self.readme_file      = "README.rdoc"
end